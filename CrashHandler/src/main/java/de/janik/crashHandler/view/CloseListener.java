package de.janik.crashHandler.view;

// <- Import ->
// <- Static_Import ->

/**
 * @author Jan.Marcel.Janik [©2014]
 *
 */
@FunctionalInterface
public interface CloseListener {
	// <- Public ->
	// <- Protected ->
	// <- Private->
	// <- Static ->
	// <- Constructor ->

	// <- Abstract ->
	void onCLose();

	// <- Object ->
	// <- Getter & Setter ->
	// <- Static ->
}
